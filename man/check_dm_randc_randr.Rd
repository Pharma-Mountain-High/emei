% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_dm_randc_randr.R
\name{check_dm_randc_randr}
\alias{check_dm_randc_randr}
\title{Check consistency of RANDC/RANDR randomization variables in DM}
\usage{
check_dm_randc_randr(DM)
}
\arguments{
\item{DM}{DM Data frame. Must contain \code{USUBJID} and, if present, randomization
variables like \verb{RANDC[1..N]} and \verb{RANDR[1..N]}.}
}
\value{
Logical scalar. On success, returns \code{TRUE} produced by \code{pass()}; on
failure, returns \code{FALSE} produced by \code{fail()} with \code{msg} and \code{data}
attributes containing the failure message and a data frame of inconsistent
records.
}
\description{
automatically detects randomization variables such as \code{RANDC1},
\code{RANDR1}, ... in the DM dataset and compares values for each corresponding
pair. If any inconsistency is found, the function returns a failure with
detailed records.
}
\examples{


DM <- data.frame(
  USUBJID = c("01", "02", "03"),
  RANDC1 = c("A", "B", "C"),
  RANDR1 = c("A", "C", "C"),
  stringsAsFactors = FALSE
)
check_dm_randc_randr(DM)


DM2 <- data.frame(
  USUBJID = c("01", "02"),
  RANDC1 = c("A", "B"),
  RANDR1 = c("A", "B"),
  stringsAsFactors = FALSE
)
check_dm_randc_randr(DM2)

}
