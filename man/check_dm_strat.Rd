% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_dm_strat.R
\name{check_dm_strat}
\alias{check_dm_strat}
\title{check missing stratification factors in randomized DM}
\usage{
check_dm_strat(DM)
}
\arguments{
\item{DM}{Data frame containing \code{USUBJID} and, if applicable, randomization
variables named like \code{RANDC1}, \code{RANDC2}, ..., and \code{RANDR1}, \code{RANDR2}, ...}
}
\value{
Logical scalar. \code{pass()} returns \code{TRUE} when no records with missing
stratification factors are found. \code{fail()} returns \code{FALSE} and carries
\code{msg} and \code{data} attributes with the failure message and the subset of
inconsistent records.
}
\description{
Detects randomization/stratification variables in DM whose names
match \verb{RANDC[1..N]} and \verb{RANDR[1..N]}. For randomized studies, flags
subjects where any of these variables are missing. If no such variables are
found in DM, the check returns a failure indicating that it is only
applicable when randomization variables exist.
}
\examples{
# Example: failing case (missing stratification values)
DM <- data.frame(
  USUBJID = c("01", "02", "03"),
  RANDC1 = c("A", NA, "C"),
  RANDR1 = c("A", "B", NA),
  stringsAsFactors = FALSE
)
check_dm_strat(DM)

# Example: passing case (no missing stratification values)
DM2 <- data.frame(
  USUBJID = c("01", "02"),
  RANDC1 = c("A", "B"),
  RANDR1 = c("A", "B"),
  stringsAsFactors = FALSE
)
check_dm_strat(DM2)

}
