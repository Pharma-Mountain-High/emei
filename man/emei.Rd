% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emei.R
\name{emei}
\alias{emei}
\title{One-Click SDTM Data Check Execution (Merge SUPP, Preprocessing, Generate Report)}
\usage{
emei(
  proj,
  folder,
  priority = c("High", "Medium", "Low"),
  type = c("ALL", "ONC", "PRO"),
  export_excel = TRUE,
  outdir = "report",
  save_rds = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{proj}{Character string, required. Project number used for output file naming (e.g., \code{QLG2198_301}).}

\item{folder}{Character string, required. SDTM data directory. The function will read all
\code{.sas7bdat} files in the directory.}

\item{priority}{Character vector or single comma-separated string. Default \code{c("High","Medium","Low")}.
If a single string is provided (e.g., "High,Medium"), it will be automatically split,
deduplicated, and ordered.}

\item{type}{Character vector or single comma-separated string. Default \code{c("ALL","ONC","PRO")}.
If a single string is provided (e.g., "ALL,ONC"), it will be automatically split,
deduplicated, and ordered.}

\item{export_excel}{Logical value, whether to export Excel report. Default \code{TRUE}.}

\item{outdir}{Character string, report output directory. Default \code{"report"}; will attempt
to create if it doesn't exist.}

\item{save_rds}{Logical value, whether to additionally save the read source data list as
\code{source_data_\{proj\}.rds}. Default \code{FALSE}.}

\item{verbose}{Logical value, whether to output runtime information. Default \code{TRUE}.}
}
\value{
Returns the result object from \code{run_all_checks()}. If \code{export_excel = TRUE},
the returned object will have an \code{outfile} attribute containing the full path to
the generated Excel file.
}
\description{
Reads SDTM \code{.sas7bdat} files from a specified directory, automatically merges SUPP datasets
for each domain, performs lightweight standardized preprocessing on \code{dm}, \code{ae}, and \code{vs}
according to established rules, then calls \code{run_all_checks()} to execute comprehensive
data checks. Optionally exports check results to an Excel file (filename consistent with
command-line tools).
}
\examples{
\dontrun{
# Minimal example: specify project number and SDTM directory
res <- emei(
  proj = "QLG2198_301",
  folder = "~/development/Projects02/QLG2198/
  QLG2198-301/SP/ole_csr/data/sdtm"
)

# Custom priority/type (supports vector or comma-separated string)
res <- emei(
  proj = "QL1706_308",
  folder = "~/development/Projects02/QL1706-308/SP/dryrun_ph2/data/sdtm",
  priority = c("High", "Medium"),
  type = "ALL,ONC"
)

# Return object only, no Excel export; also save source data RDS
res <- emei(
  proj = "QL1706_308",
  folder = "~/development/Projects02/QL1706-308/SP/dryrun_ph2/data/sdtm",
  export_excel = FALSE,
  save_rds = TRUE
)
}
}
