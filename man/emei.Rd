% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emei.R
\name{emei}
\alias{emei}
\title{SDTM 数据检查一键执行（合并 SUPP、预处理、生成报告）}
\usage{
emei(
  proj,
  folder,
  priority = c("High", "Medium", "Low"),
  type = c("ALL", "ONC", "PRO"),
  export_excel = TRUE,
  outdir = "report",
  save_rds = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{proj}{字符串，必填。项目编号，用于输出文件命名（例如 \code{QLG2198_301}）。}

\item{folder}{字符串，必填。SDTM 数据目录，函数会读取目录下所有 \code{.sas7bdat} 文件。}

\item{priority}{字符向量或单个逗号分隔字符串。默认 \code{c("High","Medium","Low")}。
若传入单个字符串（如 "High,Medium"），会自动拆分、去重并保序。}

\item{type}{字符向量或单个逗号分隔字符串。默认 \code{c("ALL","ONC","PRO")}。
若传入单个字符串（如 "ALL,ONC"），会自动拆分、去重并保序。}

\item{export_excel}{逻辑值，是否导出 Excel 报告，默认 \code{TRUE}。}

\item{outdir}{字符串，报告输出目录，默认 \code{"report"}；不存在将尝试创建。}

\item{save_rds}{逻辑值，是否额外保存读取到的源数据列表为 \code{source_data_\{proj\}.rds}，默认 \code{FALSE}。
，默认 \code{FALSE}。}

\item{verbose}{逻辑值，是否输出运行信息，默认 \code{TRUE}。}
}
\value{
返回 \code{run_all_checks()} 的结果对象。若 \code{export_excel = TRUE}，
将为返回对象附加属性 \code{outfile}，其值为生成的 Excel 文件的完整路径。
}
\description{
从指定目录读取 SDTM \code{.sas7bdat} 文件，自动合并各域的 SUPP 数据集，按既定规则对
\code{dm}、\code{ae}、\code{vs} 进行轻量标准化预处理，随后调用 \code{run_all_checks()} 执行
全量数据检查；可选将检查结果导出为 Excel 文件（文件名与命令行工具保持一致）。
}
\examples{
\dontrun{
# 最小示例：指定项目编号与 SDTM 目录
res <- emei(
  proj = "QLG2198_301",
  folder = "~/development/Projects02/QLG2198/QLG2198-301/SP/ole_csr/data/sdtm"
)

# 自定义 priority/type（支持向量或逗号分隔字符串）
res <- emei(
  proj = "QL1706_308",
  folder = "~/development/Projects02/QL1706-308/SP/dryrun_ph2/data/sdtm",
  priority = c("High", "Medium"),
  type = "ALL,ONC"
)

# 仅返回对象，不导出 Excel；同时保存源数据 RDS
res <- emei(
  proj = "QL1706_308",
  folder = "~/development/Projects02/QL1706-308/SP/dryrun_ph2/data/sdtm",
  export_excel = FALSE,
  save_rds = TRUE
)
}
}
