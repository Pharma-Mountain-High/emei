% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_supp.R
\name{merge_supp}
\alias{merge_supp}
\title{Merge SUPP with main SDTM domain; return \code{data} when SUPP is empty}
\usage{
merge_supp(data, supp)
}
\arguments{
\item{data}{data frame. Parent SDTM domain (e.g., AE, CM, DM, ...)}

\item{supp}{data frame. Supplemental qualifiers for the domain (e.g., SUPPAE)}
}
\value{
data frame. Merged dataset; or original \code{data} when \code{supp} is empty
}
\description{
This function merges a supplemental qualifiers dataset (SUPPxx) into its
corresponding parent SDTM domain. If \code{supp} has 0 rows, the original \code{data}
is returned unchanged.
}
\details{
Required columns in \code{supp}:
\itemize{
\item STUDYID, RDOMAIN, USUBJID, QNAM, QVAL
\item For non-DM domains also require: IDVAR, IDVARVAL (used to recover SEQ)
}

If \code{QLABEL} exists in \code{supp}, variable labels are applied to the new columns.
}
\examples{
# Example 1: Empty SUPP → returns original data
dm <- data.frame(STUDYID = "S1", USUBJID = c("01", "02"), DOMAIN = "DM")
suppdm <- dm[0, ] # 0-row SUPPDM
merge_supp(dm, suppdm)

# Example 2: Non-DM with minimal SUPP
ae <- data.frame(
  STUDYID = "S1",
  USUBJID = c("01", "01"),
  DOMAIN = "AE",
  AESEQ = c(1, 2),
  stringsAsFactors = FALSE
)
suppae <- data.frame(
  STUDYID = "S1",
  RDOMAIN = "AE",
  USUBJID = c("01", "01"),
  IDVAR = "AESEQ",
  IDVARVAL = c("1", "2"),
  QNAM = c("AESOC", "AESEV"),
  QVAL = c("SOC1", "MILD"),
  stringsAsFactors = FALSE
)
merge_supp(ae, suppae)

}
