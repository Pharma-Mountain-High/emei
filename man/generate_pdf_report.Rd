% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_pdf_report.R
\name{generate_pdf_report}
\alias{generate_pdf_report}
\title{Generate a PDF report from sdtmchecks results}
\usage{
generate_pdf_report(
  all_rec,
  output_file = "sdtmchecks_report.pdf",
  mystudy = "Unknown",
  category = c("ALL", "ONC", "PRO"),
  priority = c("High", "Medium", "Low"),
  data_path = getwd(),
  server_name = "Local",
  rmd_path = NULL
)
}
\arguments{
\item{all_rec}{List. Check results as returned by
\code{sdtmchecks::run_all_checks()}, or loaded from an
.rds file previously saved by the Shiny app.}

\item{output_file}{Character. Path for the output PDF file. If only a
filename is given it will be created in the current
working directory.}

\item{mystudy}{Character. Study identifier displayed in the report
header (default: \code{"Unknown"}).}

\item{category}{Character vector. Check categories to include, e.g.
\code{c("ALL", "ONC", "PRO")}.
Defaults to all categories.}

\item{priority}{Character vector. Check priorities to include, e.g.
\code{c("High", "Medium", "Low")}. Defaults to all.}

\item{data_path}{Character. The SDTMv data path that was used when the
checks were run (shown in the report header).}

\item{server_name}{Character. Server display name for the report header
(default: \code{"Local"}).}

\item{rmd_path}{Character. Path to the \code{Emei.Rmd} template.
By default it looks for the file in the same directory
as this script.}
}
\value{
Invisibly returns the path to the generated PDF file.
}
\description{
This function takes the output of \code{Emei::run_all_checks()} and
renders it into a landscape-oriented PDF report using the Emei.Rmd
template.
}
